export default function AgWizPanel() {
  return (
    <div className="panel-algo">
      <h4>Algorithm Visualizer</h4>
      <p>
        Final uni project. A desktop tool built in Godot that lets users
        write Python code which is then executed and visualized in real time
        as animated graph/tree structures.
      </p>
      <p>Integrated a Python runtime directly into Godot via GDExtension bindings.</p>
    </div>
  )
}